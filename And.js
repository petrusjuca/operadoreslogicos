// AND ( && ) - Só é verdade, se as duas condicoes forem vdd. 
// - programa que ve se 100 moedas coletadas e 1 estrela

let moedas = 100
let item = "estrela"

let resultado = (moedas coletadas = 100) && (item === "Estrela")
console.log(resultado)
// vai sair falso
